import { HeartOutlined } from "@ant-design/icons";

const Like = () => {
	// Get list of people who liked the post
	const getLikes = () => {};

	// Get if user has liked the post
	const getLiked = () => {};

	// Like the post
	const like = () => {};

	// Unlike the post
	const unlike = () => {};

	return <HeartOutlined style={{ fontSize: "2rem" }} />;
};

export default Like;
